<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gcheesescheesypub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GCheesesCheesyPub">

        <activity android:name=".DessertsActivity"
            android:label="Yummy Desserts"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".StartersActivity"
            android:label="Small Plates"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".MainCoursesActivity"
            android:label="The Cheese's Main Dishes"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>